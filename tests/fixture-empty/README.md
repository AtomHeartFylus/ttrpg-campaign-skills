# Empty campaign fixture

Deliberately almost empty: `tests/evals/campaign-setup.md` is the one eval that runs on a folder
with nothing in it, because "there is no profile here" is the branch the setup interview exists
for. This README is the only file, and the skill under test may ignore it.

Do not add a profile here. A fixture that already answers the question tests nothing.
