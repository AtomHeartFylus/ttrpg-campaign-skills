---
name: Skill defect
about: A skill produced something wrong, or refused when it should not have
labels: defect
---

**Which skill, which version** (`metadata.version` from its `SKILL.md`):

**What you asked** (the prompt, verbatim if you can):

**What it produced** (the artifact, or the relevant part):

**What it should have produced, and which rule says so** (a phase, a required element, a principle
tag, a profile slot):

**Profile context** — the answers that should have governed this (`D.shape`, `B.distance`,
`A.resource`, any `E.overrides`). Please do not paste material about real people; the slot values
are enough:

**Does `python scripts/validate_profile.py <your profile>` report anything?**
